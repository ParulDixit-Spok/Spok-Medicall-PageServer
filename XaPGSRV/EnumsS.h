#pragma once


// enumerations that are Shared/Included in both project types
//  pageserver (xn, xo, xa), and configuration utility.


enum {BY_PAGERID = 0, BY_PROFILEID, BY_PATIENTID};

enum {FAIL = 0, SUCCESS};
